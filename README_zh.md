# EdgeWeave 前端

基于 React 19 和 Vite 8 构建的 EdgeWeave 节点/订阅管理现代 Web 界面。

_[English](README.md)_

## 项目特点

- 📊 **仪表盘**：节点、订阅及系统状态概览。
- 🔗 **节点管理**：节点的完整 CRUD 操作，支持批量导入和文件上传。
- 🔄 **订阅管理**：管理订阅源，支持实时同步和历史记录追踪。
- 🛠️ **输出配置**：生成兼容 Mihomo (Clash) 的配置，支持预览、渲染及版本管理。
- 📏 **规则管理**：支持规则模板 (ACL4SSR) 及自定义规则集。
- 🔐 **认证系统**：安全的基于 JWT 的登录及用户信息管理。
- 🎨 **现代 UI**：基于 Tailwind CSS 和 Radix UI Slot 构建的简洁响应式界面。
- 🚀 **卓越性能**：由 Vite 8 beta 和 React 19 驱动，提供极速的开发和用户体验。

## 技术栈

- **框架**: [React 19](https://react.dev/)
- **构建工具**: [Vite 8 (Beta)](https://vite.dev/)
- **样式**: [Tailwind CSS 3](https://tailwindcss.com/)
- **状态管理**: [Zustand](https://github.com/pmndrs/zustand)
- **数据请求**: [Alova](https://alova.js.org/)
- **图标**: [Lucide React](https://lucide.dev/)
- **组件库**: [Radix UI Slot](https://www.radix-ui.com/docs/primitives/components/slot)
- **包管理器**: [pnpm](https://pnpm.io/)

## 项目结构

```
edgeweave-frontend/
├── public/              # 静态资源
├── src/
│   ├── assets/          # 图片及字体
│   ├── components/      # 可复用 UI 组件
│   │   ├── auth/        # 权限守卫
│   │   ├── layout/      # 侧边栏、顶栏等
│   │   └── ui/          # 基础 UI 元件 (Radix)
│   ├── lib/
│   │   ├── api/         # Alova API 实例及方法
│   │   └── utils.ts     # 工具函数
│   ├── pages/           # 应用视图/路由
│   ├── store/           # Zustand 状态存储
│   ├── styles/          # 全局 CSS 及 Tailwind 配置
│   ├── App.tsx          # 根组件及路由配置
│   └── main.tsx         # 应用入口点
├── .env                 # 环境变量
├── package.json         # 依赖及脚本
├── tailwind.config.js   # Tailwind 配置
└── vite.config.ts       # Vite 配置
```

## 快速开始

### 准备工作

- [Node.js](https://nodejs.org/) (推荐版本 22 或更高)
- [pnpm](https://pnpm.io/installation) (版本 10 或更高)

### 安装

1. 克隆仓库
2. 安装依赖：

```bash
pnpm install
```

### 配置

创建 `.env` 文件（或从 `.env.development` 复制）并配置后端 API 地址：

```env
VITE_PROXY_HOST=http://localhost:25610
VITE_USE_MOCK=false
```

### 开发环境

运行开发服务器：

```bash
pnpm dev
```

应用将运行在 `http://localhost:5173`（或环境变量中指定的端口）。

### 生产构建

构建生产版本：

```bash
pnpm build
```

构建后的文件将存放在 `dist/` 目录中。

### 代码检查与格式化

```bash
# 代码检查
pnpm lint

# 格式化
pnpm format
```

## 许可证

本项目采用 MIT 许可证。

Copyright © 2025 ydfk.
